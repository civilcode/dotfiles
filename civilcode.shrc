source $HOME/.civilcode.git
source $HOME/.civilcode.erlang
source $HOME/.civilcode.elixir
source $HOME/.civilcode.docker
source $HOME/.civilcode.slack

# custom shell

[[ -f ~/.shrc.local ]] && source ~/.shrc.local
